const { getDarwinDataFromS3Bucket: s3Example } = require("./s3-example");

/**
 * This example uses the s3 bucket from Darwin
 */
s3Example();

/**
 * This example uses STOP from Darwin Live Push Port
 */
// just node run the darwin-live.js file for reference
